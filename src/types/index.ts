export type StringType = string;

export type PreparInfoType = {
    id: number;
    bold: string;
    normal: string;
}

export type IngredientsType = {
    id: number;
    text: string;
}

export type InstructionsType = {
    id: number;
    bold: string;
    normal: string;
}

export type NutritionsType = {
    id: number;
    serving: string;
    value: string;
    hr: boolean
}